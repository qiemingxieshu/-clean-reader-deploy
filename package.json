{
  "name": "clean-reader",
  "version": "1.0.0",
  "description": "A clean web reader that extracts article content from any URL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "icons": "node scripts/generate-icons.js",
    "android": "npx cap init && npx cap add android && npx cap run android"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^24.0.0",
    "cors": "^2.8.5",
    "canvas": "^2.11.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
